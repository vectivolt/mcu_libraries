.pio/
build/
demo/.pio/
demo/build/
.vscode/
.DS_Store
*.o
*.elf
*.bin
*.tmp
.claude/
ui/node_modules/
ui/dist/

# Not part of VectiSuite: a separate product with its own git remote that
# happens to sit under libraries/. Excluded so it can never be swept into a
# commit here by `git add -A`.
libraries/cblicense/
